# orbital-orbit

Yuanhang Liu 的个人主页（Astro 静态站点）：首页 Portfolio、`/about` 关于页。

## 命令

```bash
npm install
npm run dev
npm run build
npm run preview
```

站点根 URL 请在 `astro.config.mjs` 的 `site` 中与实际上线地址保持一致。
